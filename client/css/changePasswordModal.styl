#changePasswordModal input, #changeEmailModal input[name="email"], #cancelAccountModal input[name="password"], #changeAccountPasswordModal input
  width 153px
  round-corner(top, right, 0px)
  round-corner(bottom, right, 0px)

#changePasswordModal button, #changeEmailModal button, #cancelAccountModal button, #changeAccountPasswordModal button
  round-corner(top, right, 15px)
  round-corner(bottom, right, 15px)
  padding-right 15px

#changeEmailModal input[name="email"]
  width 183px

#changeAccountPasswordModal input[name="currentPassword"]
  width 223px
  round-corner(top, right, 15px)
  round-corner(bottom, right, 15px)
